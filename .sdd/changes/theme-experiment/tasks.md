# Tasks: Theme Switcher Experiment

- `[ ]` Create `src/store/useThemeStore.ts` with Zustand and persistence.
- `[ ]` Define CSS variables in `src/app/globals.css` for both `[data-theme="glass"]` and `[data-theme="sleek"]`.
- `[ ]` Implement a `ThemeWatcher` component to apply the theme attribute to the HTML tag (hydration-safe).
- `[ ]` Create `src/components/common/ThemeToggle.tsx` with Framer Motion.
- `[ ]` Integrate `ThemeWatcher` in the root `Layout.tsx`.
- `[ ]` Verify theme switching works and persists across reloads.
